name: flutter_sticky_header
description: Flutter implementation of sticky headers as a sliver. Use it in a CustomScrollView.
version: 0.7.0
homepage: https://github.com/letsar/flutter_sticky_header

environment:
  sdk: '>=2.17.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  # value_layout_builder: ^0.4.0
  value_layout_builder:
    git:
      url: https://github.com/majedNaifeh/value_layout_builder.git
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter
  sliver_tools: ^0.2.7


